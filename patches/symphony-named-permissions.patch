diff --git a/elixir/lib/symphony_elixir/config/schema.ex b/elixir/lib/symphony_elixir/config/schema.ex
--- a/elixir/lib/symphony_elixir/config/schema.ex
+++ b/elixir/lib/symphony_elixir/config/schema.ex
@@ -112,6 +112,7 @@ defmodule SymphonyElixir.Config.Schema do
       )
 
+      field(:permissions, :string)
       field(:thread_sandbox, :string, default: "workspace-write")
       field(:turn_sandbox_policy, :map)
       field(:turn_timeout_ms, :integer, default: 3_600_000)
@@ -126,6 +127,7 @@ defmodule SymphonyElixir.Config.Schema do
           :command,
           :approval_policy,
+          :permissions,
           :thread_sandbox,
           :turn_sandbox_policy,
           :turn_timeout_ms,
diff --git a/elixir/lib/symphony_elixir/config.ex b/elixir/lib/symphony_elixir/config.ex
--- a/elixir/lib/symphony_elixir/config.ex
+++ b/elixir/lib/symphony_elixir/config.ex
@@ -23,6 +23,7 @@ defmodule SymphonyElixir.Config do
   @type codex_runtime_settings :: %{
           approval_policy: String.t() | map(),
+          permissions: String.t() | nil,
           thread_sandbox: String.t(),
           turn_sandbox_policy: map()
         }
@@ -97,6 +98,7 @@ defmodule SymphonyElixir.Config do
          %{
            approval_policy: settings.codex.approval_policy,
+           permissions: settings.codex.permissions,
            thread_sandbox: settings.codex.thread_sandbox,
            turn_sandbox_policy: turn_sandbox_policy
          }}
diff --git a/elixir/lib/symphony_elixir/codex/app_server.ex b/elixir/lib/symphony_elixir/codex/app_server.ex
--- a/elixir/lib/symphony_elixir/codex/app_server.ex
+++ b/elixir/lib/symphony_elixir/codex/app_server.ex
@@ -16,6 +16,7 @@ defmodule SymphonyElixir.Codex.AppServer do
           metadata: map(),
           approval_policy: String.t() | map(),
           auto_approve_requests: boolean(),
+          permissions: String.t() | nil,
           thread_sandbox: String.t(),
           turn_sandbox_policy: map(),
           thread_id: String.t(),
@@ -55,6 +56,7 @@ defmodule SymphonyElixir.Codex.AppServer do
            metadata: metadata,
            approval_policy: session_policies.approval_policy,
            auto_approve_requests: session_policies.approval_policy == "never",
+           permissions: session_policies.permissions,
            thread_sandbox: session_policies.thread_sandbox,
            turn_sandbox_policy: session_policies.turn_sandbox_policy,
            thread_id: thread_id,
@@ -77,6 +79,7 @@ defmodule SymphonyElixir.Codex.AppServer do
           approval_policy: approval_policy,
           auto_approve_requests: auto_approve_requests,
+          permissions: permissions,
           turn_sandbox_policy: turn_sandbox_policy,
           thread_id: thread_id,
           workspace: workspace,
@@ -95,7 +98,15 @@ defmodule SymphonyElixir.Codex.AppServer do
         })
 
-    case start_turn(port, thread_id, prompt, issue, workspace, approval_policy, turn_sandbox_policy) do
+    case start_turn(
+           port,
+           thread_id,
+           prompt,
+           issue,
+           workspace,
+           approval_policy,
+           permissions,
+           turn_sandbox_policy
+         ) do
       {:ok, turn_id} ->
         session_id = "#{thread_id}-#{turn_id}"
         Logger.info("Codex session started for #{issue_context(issue)} session_id=#{session_id}")
@@ -307,18 +318,26 @@ defmodule SymphonyElixir.Codex.AppServer do
   defp start_thread(
          port,
          workspace,
-         %{approval_policy: approval_policy, thread_sandbox: thread_sandbox},
+         %{
+           approval_policy: approval_policy,
+           permissions: permissions,
+           thread_sandbox: thread_sandbox
+         },
          dynamic_tool_binding
        ) do
+    params =
+      %{
+        "approvalPolicy" => approval_policy,
+        "cwd" => workspace,
+        "dynamicTools" => dynamic_tool_binding.tool_specs
+      }
+      |> put_thread_permissions(permissions, thread_sandbox)
+
     send_message(port, %{
       "method" => "thread/start",
       "id" => @thread_start_id,
-      "params" => %{
-        "approvalPolicy" => approval_policy,
-        "sandbox" => thread_sandbox,
-        "cwd" => workspace,
-        "dynamicTools" => dynamic_tool_binding.tool_specs
-      }
+      "params" => params
     })
 
     case await_response(port, @thread_start_id) do
@@ -334,8 +353,26 @@ defmodule SymphonyElixir.Codex.AppServer do
     end
   end
 
-  defp start_turn(port, thread_id, prompt, issue, workspace, approval_policy, turn_sandbox_policy) do
+  defp put_thread_permissions(params, permissions, _thread_sandbox)
+       when is_binary(permissions) and permissions != "" do
+    Map.put(params, "permissions", permissions)
+  end
+
+  defp put_thread_permissions(params, _permissions, thread_sandbox) do
+    Map.put(params, "sandbox", thread_sandbox)
+  end
+
+  defp start_turn(
+         port,
+         thread_id,
+         prompt,
+         issue,
+         workspace,
+         approval_policy,
+         permissions,
+         turn_sandbox_policy
+       ) do
+    params =
+      %{
+        "threadId" => thread_id,
+        "input" => [
+          %{
+            "type" => "text",
+            "text" => prompt
+          }
+        ],
+        "cwd" => workspace,
+        "title" => "#{issue.identifier}: #{issue.title}",
+        "approvalPolicy" => approval_policy
+      }
+      |> put_turn_permissions(permissions, turn_sandbox_policy)
+
     send_message(port, %{
       "method" => "turn/start",
       "id" => @turn_start_id,
-      "params" => %{
-        "threadId" => thread_id,
-        "input" => [
-          %{
-            "type" => "text",
-            "text" => prompt
-          }
-        ],
-        "cwd" => workspace,
-        "title" => "#{issue.identifier}: #{issue.title}",
-        "approvalPolicy" => approval_policy,
-        "sandboxPolicy" => turn_sandbox_policy
-      }
+      "params" => params
     })
 
     case await_response(port, @turn_start_id) do
@@ -344,6 +381,15 @@ defmodule SymphonyElixir.Codex.AppServer do
     end
   end
+
+  defp put_turn_permissions(params, permissions, _turn_sandbox_policy)
+       when is_binary(permissions) and permissions != "" do
+    Map.put(params, "permissions", permissions)
+  end
+
+  defp put_turn_permissions(params, _permissions, turn_sandbox_policy) do
+    Map.put(params, "sandboxPolicy", turn_sandbox_policy)
+  end
 
   defp await_turn_completion(port, on_message, tool_executor, auto_approve_requests) do
